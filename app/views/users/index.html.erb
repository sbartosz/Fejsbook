<h1>Społeczność Facebook'a</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_image_tag user.email, :size => 30 %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
